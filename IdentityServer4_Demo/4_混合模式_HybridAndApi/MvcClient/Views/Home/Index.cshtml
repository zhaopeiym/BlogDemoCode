@{
    ViewData["Title"] = "Home Page";
}
<span>我是客户端MvcClient</span>
<a href="/home/Logout">退出</a>
<div>
    <a href="/home/getapi">GetApi</a>
</div>
<dl>
    @if (User != null && User.Claims != null)
    {
        foreach (var claim in User.Claims)
        {
            <dt>@claim.Type</dt>
            <dd>@claim.Value</dd>

        }
    }
</dl>